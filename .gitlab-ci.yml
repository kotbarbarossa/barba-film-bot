stages:
  - lint
  - build
  - deploy
  - tag

variables:
  IMAGE: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA
  IMAGE_LATEST: $CI_REGISTRY_IMAGE:latest

# ──────────────────────────────────────────────
# Lint
# ──────────────────────────────────────────────
lint:
  stage: lint
  image: python:3.13-slim
  before_script:
    - pip install uv --quiet
  script:
    - uv run --frozen ruff check .
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "dev"

# ──────────────────────────────────────────────
# Build & push image to GitLab Container Registry
# ──────────────────────────────────────────────
build:
  stage: build
  image: docker:27-cli
  services:
    - docker:27-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $IMAGE -t $IMAGE_LATEST .
    - docker push $IMAGE
    - docker push $IMAGE_LATEST
  rules:
    - if: $CI_COMMIT_BRANCH == "main"

# ──────────────────────────────────────────────
# Deploy to AWS EC2 via SSH
#
# Required GitLab CI/CD variables (Settings → CI/CD → Variables):
#   SSH_PRIVATE_KEY  — EC2 SSH private key (PEM, protected)
#   DEPLOY_HOST      — EC2 public IP or hostname
#   DEPLOY_USER      — SSH user (ubuntu / ec2-user)
#   DEPLOY_DIR       — project directory on server (e.g. /home/ubuntu/barba-film-bot)
# ──────────────────────────────────────────────
deploy:
  stage: deploy
  image: alpine:3.20
  before_script:
    - apk add --no-cache openssh-client bash
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh && chmod 700 ~/.ssh
    - ssh-keyscan -H $DEPLOY_HOST >> ~/.ssh/known_hosts
  script:
    - scp docker-compose.yml $DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_DIR/docker-compose.yml
    - |
      ssh $DEPLOY_USER@$DEPLOY_HOST "
        set -e
        cd $DEPLOY_DIR
        docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
        APP_IMAGE=$IMAGE docker compose pull
        APP_IMAGE=$IMAGE docker compose up -d --remove-orphans
        docker image prune -f
      "
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
  environment:
    name: production

# ──────────────────────────────────────────────
# Tag release from pyproject.toml version
# ──────────────────────────────────────────────
tag:
  stage: tag
  image: python:3.13-slim
  before_script:
    - pip install uv --quiet
  script:
    - VERSION=$(uv run python -c "from app.core.version import VERSION; print(VERSION)")
    - git config user.email "ci@gitlab.com"
    - git config user.name "GitLab CI"
    - git remote set-url origin "https://oauth2:${CI_JOB_TOKEN}@${CI_SERVER_HOST}/${CI_PROJECT_PATH}.git"
    - git tag "v${VERSION}" || echo "Tag v${VERSION} already exists, skipping"
    - git push origin "v${VERSION}" || true
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
